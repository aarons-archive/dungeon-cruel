// code to open door

if (active == true)
{
	if (start_Y - sprite_height < y) { y -= 0.5; }
}


if (keyActive == true)
{
	if (start_Y - sprite_height < y) { y -= 0.5; }
}

if (plateActive == true)
{
	if (start_Y - sprite_height < y) { y -= 0.5; }
}